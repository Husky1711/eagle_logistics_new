ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-in-production
SESSION_SECRET=generate-a-long-random-string
# Prefer leaving CONTENT_DIR / REPO_ROOT unset (absolute defaults in config.py).
# If set, use absolute paths, or paths relative to the backend/ directory.
CORS_ORIGINS=http://localhost:5174,http://localhost:5175,http://localhost:5176,http://127.0.0.1:5174,http://127.0.0.1:5175,http://127.0.0.1:5176
